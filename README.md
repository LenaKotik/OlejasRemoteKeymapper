# OlejasRemoteKeymapper
a desktop application to send remapped keyboard inputs over local network
*TODO:* (for me)
  * config files for remapping
  * some special keys are not supported
  * mouse support
  * use local tunnels, they might be faster
